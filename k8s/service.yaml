apiVersion: v1
kind: Service

metadata:
  name: sample-app-service
  namespace: project01

spec:
  type: NodePort
  selector:
    app: sample-app
  ports:
    - protocol: TCP
      port: 5000123
      
      targetPort: 5000